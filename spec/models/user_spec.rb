require 'rails_helper'

describe User do
  it "can add songs from spotify id"
  # user.add_song spotify_id => song
  # song is valid (has artist and title) if id was valid
  # doesn't error if id is invalid

  it "can vote for a song"
  # vote_for returns true if added, false if out of votes
end